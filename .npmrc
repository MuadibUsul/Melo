# pnpm: hoist nothing implicitly; rely on explicit deps per package.
auto-install-peers=true
strict-peer-dependencies=false
# Keep the store fast and deterministic across the workspace.
prefer-workspace-packages=true
link-workspace-packages=deep
